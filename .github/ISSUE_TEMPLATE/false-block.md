---
name: False block
about: weft rejected something that is not actually broken. This is the one bug we fix first.
title: "False block: "
labels: false-block
---

**The verdict**

Paste the `REJECT` line (or `weft check --format=json` output).

**Why it is not actually broken**

Where the referenced thing is declared or defined, and how the code resolves it.

**Reproduction**

A minimal file pair, or a public repo and commit, that shows it.

**Environment**

- weft version (`weft --version`):
- Python version:
- stack (`weft index --status`):
