# Demo: follow the evidence

[Watch the 78-second, 1080p motion film](https://github.com/Avinash-Amudala/weftgate/releases/download/v0.2.0/weftgate-demo.mp4).
An animated neural brain, evidence paths, repair sequence, compact context card and
stale-memory transition explain how Weftgate helps agents understand, remember and verify. The film includes original synthesized
sound design; every explanation is also visible on screen, so it works muted.
[WebVTT captions](assets/demo.vtt), a [lightweight GIF](assets/demo.gif), and a
[storyboard](assets/demo-storyboard.jpg) are included.

This is an animated walkthrough, not a screen recording. The renderer runs the real
public CLI against a temporary fixture containing three intentional mistakes. It
records the exit codes and JSON in [demo-evidence.json](assets/demo-evidence.json)
and uses the actual suggestions and verdicts in the animation. The context card is
queried through the CLI, and the memory chapter saves a note, recalls it, changes its
source and verifies that default recall withholds it. The displayed JSON byte count
comes from that run; it is not an invented token-saving percentage. These are fixture
results, not a claim of production accuracy or a guarantee about another project.

## Reproduce

```bash
pip install -e '.[demo]'
python -m scripts.make_brain_demo --python "$(command -v python)" --out docs/assets
# Optional: review the storyboard before rendering the video.
python -m scripts.make_brain_demo --stills-only --out /tmp/weftgate-storyboard
```

Rendering uses Pillow and imageio-ffmpeg only in the optional `demo` extra. The
78-second MP4 is 1920 × 1080 at 30 fps, with H.264 video, AAC stereo audio, and
fast-start playback. On Linux, install DejaVu Sans and DejaVu Sans Mono fonts.
No repository contents are uploaded by the renderer. Rendering regenerates the
fixture evidence and checks the broken, fixed, and dynamic outcomes before export.

## Transcript

- 0–5 s: The code looks right. But does it connect? Three tiny typos break references.
- 5–11 s: A neural brain introduces understand, remember and verify.
- 11–24 s: Trace `requestz`, `DATABSE_URL`, and `helth` to repository evidence.
  The gate rejects the references and suggests `requests`, `DATABASE_URL`, and `health`.
- 24–33 s: Apply the suggested names. The broken-reference count falls to zero.
  The same CLI returns `accept`, exit code 0. This is not a claim that application tests passed.
- 33–41 s: A dynamic environment key returns `review`, exit code 0. Uncertainty stays advisory.
- 41–48 s: Codex, Claude Code, Cursor and Antigravity share the verification engine.
- 48–58 s: A route contract card returns source pointers and a measured payload size.
- 58–68 s: Save a decision, change its source, and see stale recall withheld.
- 68–78 s: Install `weftgate`, configure and activate editor hooks, and keep CI required.

Sources: [film renderer](../scripts/make_brain_demo.py),
[brain geometry](../scripts/brain_visual.py) and [shared motion/sound](../scripts/make_demo.py).
Geometry, animation, and sound are generated locally; no stock assets, external music, or voices
are used. Asset license: Apache-2.0, as for the repository. Locally installed fonts
are rendered but not distributed.

## v0.2 visual identity

The brain artwork is generated by `scripts/brain_visual.py` and reused by the film.
The original geometry and synthesized music need no stock license. This is an animated
walkthrough, not a recording of live editor sessions. Native hook activation depends
on the client trust flow documented in [AGENTS-INTEGRATION.md](AGENTS-INTEGRATION.md).
